(ns metaprogramming-test
  (include test)

  (st "metaprogramming"
  

    ;; (test/t "Quote"
    ;;     (quote ()) '()
    ;; )

      (t "Quasiquote"
         `(some-fn ~(* 2 3) 4) '(some-fn 6 4))))