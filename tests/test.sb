(ns test
  (def t (fn [name test result]
           (if (not (= test result))
             (prn "❗️ Test failed: `" name
                    "` was expected to be " result " but got " test " instead. \n")
             )))
;;
  )

;; (ns test-test ;; main
;;   (include test)

;;   (test/t
;;    "Name of the test"
;;    (+ 6 5)
;;    12)
;;   )