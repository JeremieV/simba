(ns test
  (def t (fn [name test result]
           (if (= test result)
            ;;  (print true)
            ;;  (print false)
             (print "Test success")
             (print "Test failed: " name
                    "was expected to be " result "but got " test "instead.")
             )))
;;
  )

(ns test-test ;; main
  (include test)

  (test/t
   "Name of the test"
   (+ 6 5)
   12)
  )