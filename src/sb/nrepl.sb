(ns nrepl
  (include base)

  (defn start []
    (prn "nREPL server started on port 58567 on host 127.0.0.1 - nrepl://127.0.0.1:58567\n"))
  
;;   (defn respond [message]
;;     :when 'condition
;;     nil)

  (start))

(ns nrepl-tests
  (include nrepl)
  (include base)

  (print "Hello")
  (start))